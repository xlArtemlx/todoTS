export interface User {
    firstName:string,
    secondName:string,
    email:string,
    photo:string,
} 
export interface ValidUser {
    firstName:boolean,
    secondName:boolean,
    email:boolean,
    photo:boolean,
}